name: heraffi
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # ⭐ أحدث إصدارات Firebase (مستقرة ومتوافقة)
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3 
  cloud_firestore: ^6.1.1
  
  # Shared Preferences
  shared_preferences: ^2.3.3
  
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  supabase_flutter: ^2.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true